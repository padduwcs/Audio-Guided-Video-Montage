# embedding_index_sample

Placeholder cho vector/index mẫu tham chiếu từ `embedding_metadata_sample.json`.

File nhị phân thật không commit vào repo. Đường dẫn contract mẫu:

```text
data/intermediate/index/visual.index
data/intermediate/embeddings/emb_text_*.npy
data/intermediate/embeddings/emb_visual_*.npy
```

Mỗi keyframe trong `clip_metadata_sample.json` có visual embedding tương ứng trong `embedding_metadata_sample.json`.
